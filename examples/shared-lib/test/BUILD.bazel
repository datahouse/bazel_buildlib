load("@dh_buildlib//ts:defs.bzl", "ts_test")

ts_test(
    name = "test",
    srcs = glob(["**/*.ts"]),
    data = [
        "data-file.txt",
    ],
    deps = [
        "//:node_modules/@types/node",
        "//shared-lib/src",
    ],
)
