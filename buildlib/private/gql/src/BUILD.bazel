load("//private:ts_library.bzl", "ts_library")

ts_library(
    name = "src",
    srcs = glob(["**/*.ts"]),
    visibility = ["//visibility:public"],
    deps = [
        "//:node_modules/@types/node",
    ],
)
