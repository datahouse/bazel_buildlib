// Babel plugins without type definitions.
// We do not need anything but the value itself, so only the bare declarations are needed.
declare module "babel-plugin-module-resolver" {}
declare module "@babel/plugin-transform-modules-commonjs" {}
