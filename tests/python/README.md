# Python incubation/staging ground

Staging ground for Python support (EXPERIMENTAL).
At this point, serves as a test to make sure things work.
Will eventually mature into an example (or the example).

**NOTE:** the current directory structure is provisional. A more idiomatic Python `src/` layout may be adopted later.
See: <https://packaging.python.org/en/latest/discussions/src-layout-vs-flat-layout/>
