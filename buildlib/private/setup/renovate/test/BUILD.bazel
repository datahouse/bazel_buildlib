load("//private:ts_library.bzl", "ts_test")

ts_test(
    name = "test",
    srcs = glob(["**/*.ts"]),
    deps = [
        "//:node_modules/jest-extended",
        "//:node_modules/json5",
        "//private/setup/renovate/src",
    ],
)
